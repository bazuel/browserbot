{
  "name": "@browserbot/backend-shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "node esbuild --prod && tsc --declaration --declarationDir dist --emitDeclarationOnly",
    "start": "node esbuild -w &&  tsc -w --declaration --declarationDir dist --emitDeclarationOnly"
  },
  "dependencies": {
    "aws-sdk": "^2.1201.0",
    "postgres": "^3.2.4",
    "dotenv": "^16.0.1"
  },
  "devDependencies": {
    "@types/node": "^18.7.13",
    "typescript": "^4.7.4",
    "esbuild": "^0.15.5"
  }
}
